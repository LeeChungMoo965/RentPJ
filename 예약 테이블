CREATE TABLE RESERVAITION (
    ID VARCHAR2(10) CONSTRAINT RK_ID_RT REFERENCES MEMBER (ID),
    carid NUMBER(10)CONSTRAINT RK_CARID_RT REFERENCES CAR (carid),
    starttime DATE,
    endtime DATE,
    REGIST_DAY DATE DEFAULT SYSDATE
);
